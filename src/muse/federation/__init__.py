"""Muse federation coordinator package.

Empty for now; the coordinator's public re-exports land in a later task
(Task 7). This file exists so `src/muse/federation/` is a proper package
that `muse.federation.nodes` can be imported from.
"""
