"""Runtimes for the 3d/generation modality.

Each runtime in this package wraps one upstream library (TripoSR's
own `tsr` package, transformers + custom code for TRELLIS, etc.) so
many models can share a single runtime class.
"""
