"""Generic runtimes for text/translation.

`hf_translation.TranslationRuntime` is the one generic class that
serves every bundled/curated translation model (m2m100, nllb,
opus-mt, madlad) via per-family dispatch. See hf_translation.py.
"""
