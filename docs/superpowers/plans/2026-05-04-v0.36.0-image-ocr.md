# v0.36.0 Implementation Plan: image/ocr

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Add the `image/ocr` modality with `POST /v1/images/ocr` (image-in / text-out), one bundled model (`trocr-base-printed`), and three curated alternatives (Nougat, TexTeller, TrOCR-handwritten).

**Architecture:** Single runtime `HFVision2SeqRuntime` over `AutoModelForVision2Seq` + `AutoProcessor`. Multipart upload reuses `decode_image_file` from `image_generation/image_input.py`. Capability flags (`supports_handwritten`, `supports_math`) are advisory only; the route does not enforce them. Streaming deferred.

**Tech Stack:** Existing. transformers, torch, PIL.

**Spec:** `docs/superpowers/specs/2026-05-04-v0.36.0-image-ocr-design.md`.

---

## File structure

| File | Change |
|------|--------|
| `src/muse/modalities/image_ocr/__init__.py` | NEW: MODALITY tag, build_router, exports |
| `src/muse/modalities/image_ocr/protocol.py` | NEW: OcrModel Protocol + OcrResult dataclass |
| `src/muse/modalities/image_ocr/codec.py` | NEW: encode_ocr() OpenAI envelope builder |
| `src/muse/modalities/image_ocr/client.py` | NEW: OcrClient |
| `src/muse/modalities/image_ocr/routes.py` | NEW: POST /v1/images/ocr handler |
| `src/muse/modalities/image_ocr/runtimes/__init__.py` | NEW |
| `src/muse/modalities/image_ocr/runtimes/hf_vision2seq.py` | NEW: HFVision2SeqRuntime |
| `src/muse/modalities/image_ocr/hf.py` | NEW: HF plugin (sniff image-to-text + repo-name) |
| `src/muse/models/trocr_base_printed.py` | NEW: bundled script |
| `src/muse/curated.yaml` | extend: 1 bundled alias + 3 URI-resolved entries |
| `.github/workflows/fresh-venv-smoke.yml` | extend: trocr-base-printed in matrix |
| `CLAUDE.md` | extend: image/ocr section |
| `tests/modalities/image_ocr/__init__.py` | NEW |
| `tests/modalities/image_ocr/test_protocol.py` | NEW |
| `tests/modalities/image_ocr/test_codec.py` | NEW |
| `tests/modalities/image_ocr/test_routes.py` | NEW |
| `tests/modalities/image_ocr/test_client.py` | NEW |
| `tests/modalities/image_ocr/test_hf_plugin.py` | NEW |
| `tests/modalities/image_ocr/runtimes/__init__.py` | NEW |
| `tests/modalities/image_ocr/runtimes/test_hf_vision2seq.py` | NEW |
| `tests/models/test_trocr_base_printed.py` | NEW |

---

## Task A: Protocol + codec + OcrClient

**Files:**
- Create: `src/muse/modalities/image_ocr/__init__.py`
- Create: `src/muse/modalities/image_ocr/protocol.py`
- Create: `src/muse/modalities/image_ocr/codec.py`
- Create: `src/muse/modalities/image_ocr/client.py`
- Test: `tests/modalities/image_ocr/test_protocol.py`
- Test: `tests/modalities/image_ocr/test_codec.py`

**Steps:**

- [ ] **A1: Protocol + dataclass**

`src/muse/modalities/image_ocr/protocol.py`:

```python
"""Protocol + dataclasses for image/ocr."""
from __future__ import annotations

from dataclasses import dataclass, field
from typing import Any, Protocol, runtime_checkable


@dataclass
class OcrResult:
    """Output of an OCR call.

    text: extracted text. For Nougat-style models, this is LaTeX-flavored
    markdown; for TrOCR, plain text. The route does not interpret the
    string structure; it surfaces it as-is.

    completion_tokens: number of tokens generated by the decoder. Used
    for the response usage block (parity with chat completions).

    metadata: free-form dict the runtime may attach (e.g., generation
    config echo, post-processing flags). Optional.
    """
    text: str
    model_id: str
    completion_tokens: int = 0
    metadata: dict[str, Any] = field(default_factory=dict)


@runtime_checkable
class OcrModel(Protocol):
    """Structural protocol any OCR backend satisfies.

    HFVision2SeqRuntime (the generic runtime) satisfies this without
    inheriting. Tests use fakes that match the signature structurally.
    """

    def ocr(
        self,
        image: Any,
        *,
        prompt: str | None = None,
        max_new_tokens: int = 512,
        num_beams: int = 1,
    ) -> OcrResult:
        """Extract text from a single image.

        `image` is a PIL.Image (the route layer decodes the multipart
        upload via the shared decode_image_file helper). `prompt` is
        an optional task hint forwarded to the processor; TrOCR ignores
        it, Nougat uses it. Returns one OcrResult.
        """
        ...
```

- [ ] **A2: Codec**

`src/muse/modalities/image_ocr/codec.py`:

```python
"""Encoding for /v1/images/ocr responses."""
from __future__ import annotations

import uuid
from typing import Any

from muse.modalities.image_ocr.protocol import OcrResult


def encode_ocr(result: OcrResult) -> dict[str, Any]:
    """Build the OpenAI-shape envelope for /v1/images/ocr.

    Mirrors /v1/audio/transcriptions: {id, model, text, usage}.
    """
    return {
        "id": f"ocr-{uuid.uuid4().hex[:24]}",
        "model": result.model_id,
        "text": result.text,
        "usage": {"completion_tokens": result.completion_tokens},
    }
```

- [ ] **A3: Client**

`src/muse/modalities/image_ocr/client.py`:

```python
"""HTTP client for /v1/images/ocr."""
from __future__ import annotations

import io
import os
from pathlib import Path
from typing import Any

import requests


_DEFAULT_SERVER = "http://localhost:8000"


class OcrClient:
    """HTTP client for the image/ocr modality."""

    def __init__(
        self,
        server_url: str | None = None,
        *,
        timeout: float = 300.0,
    ) -> None:
        url = (
            server_url
            or os.environ.get("MUSE_SERVER")
            or _DEFAULT_SERVER
        )
        self.server_url = url.rstrip("/")
        self._timeout = timeout

    def ocr(
        self,
        image: bytes | str | Path | Any,
        *,
        model: str | None = None,
        prompt: str | None = None,
        max_new_tokens: int | None = None,
    ) -> dict[str, Any]:
        """Send an OCR request.

        `image` accepts bytes, a path-like, or a file-like object with
        .read(). PIL.Image is also accepted (encoded as PNG).
        """
        files = {"image": ("image.png", _to_bytes(image), "image/png")}
        data: dict[str, Any] = {}
        if model is not None:
            data["model"] = model
        if prompt is not None:
            data["prompt"] = prompt
        if max_new_tokens is not None:
            data["max_new_tokens"] = str(max_new_tokens)

        r = requests.post(
            f"{self.server_url}/v1/images/ocr",
            files=files, data=data, timeout=self._timeout,
        )
        r.raise_for_status()
        return r.json()


def _to_bytes(image: Any) -> bytes:
    """Coerce bytes/path/file-like/PIL.Image to bytes."""
    if isinstance(image, bytes):
        return image
    if isinstance(image, (str, Path)):
        return Path(image).read_bytes()
    if hasattr(image, "read"):
        return image.read()
    # PIL.Image fallback: serialize as PNG
    if hasattr(image, "save"):
        buf = io.BytesIO()
        image.save(buf, format="PNG")
        return buf.getvalue()
    raise TypeError(f"unsupported image type: {type(image).__name__}")
```

- [ ] **A4: __init__.py**

`src/muse/modalities/image_ocr/__init__.py`:

```python
"""image/ocr modality.

Public surface:
  - MODALITY: str (MIME-shaped tag; used by discover_modalities)
  - build_router(registry) -> APIRouter
  - OcrResult dataclass
  - OcrModel Protocol
  - OcrClient

Wire contract: POST /v1/images/ocr (multipart in, JSON out).
"""
from muse.modalities.image_ocr.client import OcrClient
from muse.modalities.image_ocr.protocol import OcrModel, OcrResult
from muse.modalities.image_ocr.routes import build_router


MODALITY = "image/ocr"


PROBE_DEFAULTS = {
    "shape": "1 small image",
    "call": lambda m: _probe_call(m),
}


def _probe_call(m):
    from PIL import Image
    img = Image.new("RGB", (64, 64), (255, 255, 255))
    return m.ocr(img, max_new_tokens=8)


__all__ = [
    "MODALITY",
    "PROBE_DEFAULTS",
    "build_router",
    "OcrModel",
    "OcrResult",
    "OcrClient",
]
```

- [ ] **A5: Tests**

Codec test (`tests/modalities/image_ocr/test_codec.py`):

```python
from muse.modalities.image_ocr.codec import encode_ocr
from muse.modalities.image_ocr.protocol import OcrResult


def test_encode_ocr_envelope_shape():
    result = OcrResult(
        text="hello world", model_id="trocr-base-printed",
        completion_tokens=4,
    )
    body = encode_ocr(result)
    assert body["model"] == "trocr-base-printed"
    assert body["text"] == "hello world"
    assert body["usage"]["completion_tokens"] == 4
    assert body["id"].startswith("ocr-")


def test_encode_ocr_id_unique_per_call():
    a = encode_ocr(OcrResult(text="x", model_id="m"))
    b = encode_ocr(OcrResult(text="x", model_id="m"))
    assert a["id"] != b["id"]


def test_encode_ocr_zero_tokens_default():
    body = encode_ocr(OcrResult(text="x", model_id="m"))
    assert body["usage"]["completion_tokens"] == 0
```

Protocol test (`tests/modalities/image_ocr/test_protocol.py`):

```python
from muse.modalities.image_ocr.protocol import OcrResult


def test_result_default_metadata_is_empty_dict():
    r = OcrResult(text="x", model_id="m")
    assert r.metadata == {}
    assert r.completion_tokens == 0


def test_result_metadata_independent_per_instance():
    """field(default_factory=dict) avoids the mutable-default trap."""
    a = OcrResult(text="a", model_id="m")
    a.metadata["k"] = 1
    b = OcrResult(text="b", model_id="m")
    assert b.metadata == {}
```

- [ ] **A6: Run + commit**

```bash
pytest tests/modalities/image_ocr/test_protocol.py tests/modalities/image_ocr/test_codec.py -q
git add src/muse/modalities/image_ocr/__init__.py \
        src/muse/modalities/image_ocr/protocol.py \
        src/muse/modalities/image_ocr/codec.py \
        src/muse/modalities/image_ocr/client.py \
        tests/modalities/image_ocr/__init__.py \
        tests/modalities/image_ocr/test_protocol.py \
        tests/modalities/image_ocr/test_codec.py
git commit -m "feat(image/ocr): protocol, codec, client skeleton

Adds the image/ocr modality skeleton: OcrModel Protocol, OcrResult
dataclass, encode_ocr OpenAI envelope, OcrClient HTTP client.

Closes #106 Task A."
```

## Task B: HFVision2SeqRuntime

**Files:**
- Create: `src/muse/modalities/image_ocr/runtimes/__init__.py`
- Create: `src/muse/modalities/image_ocr/runtimes/hf_vision2seq.py`
- Test: `tests/modalities/image_ocr/runtimes/test_hf_vision2seq.py`

**Steps:**

- [ ] **B1: Runtime**

`src/muse/modalities/image_ocr/runtimes/hf_vision2seq.py`:

```python
"""HFVision2SeqRuntime: generic OCR runtime over AutoModelForVision2Seq.

Wraps `transformers.AutoModelForVision2Seq` + `AutoProcessor`. Works
for TrOCR, Nougat, TexTeller, GOT-OCR, and any vision-encoder + text-
decoder pair. The processor's per-family preprocessing
(TrOCRProcessor, NougatProcessor) is dispatched via AutoProcessor.

Deferred imports follow the muse pattern: torch, AutoModelForVision2Seq,
AutoProcessor as module-top sentinels populated by _ensure_deps().
Tests patch the sentinels directly.
"""
from __future__ import annotations

import logging
from typing import Any

from muse.core.runtime_helpers import (
    LoadTimer, dtype_for_name, select_device, set_inference_mode,
)
from muse.modalities.image_ocr.protocol import OcrResult


logger = logging.getLogger(__name__)


torch: Any = None
AutoModelForVision2Seq: Any = None
AutoProcessor: Any = None


def _ensure_deps() -> None:
    global torch, AutoModelForVision2Seq, AutoProcessor
    if torch is None:
        try:
            import torch as _t
            torch = _t
        except Exception as e:  # noqa: BLE001
            logger.debug("HFVision2SeqRuntime torch unavailable: %s", e)
    if AutoModelForVision2Seq is None:
        try:
            from transformers import AutoModelForVision2Seq as _m
            AutoModelForVision2Seq = _m
        except Exception as e:  # noqa: BLE001
            logger.debug(
                "HFVision2SeqRuntime AutoModelForVision2Seq unavailable: %s", e,
            )
    if AutoProcessor is None:
        try:
            from transformers import AutoProcessor as _p
            AutoProcessor = _p
        except Exception as e:  # noqa: BLE001
            logger.debug("HFVision2SeqRuntime AutoProcessor unavailable: %s", e)


class HFVision2SeqRuntime:
    """Generic vision-encoder + text-decoder OCR runtime."""

    model_id: str

    def __init__(
        self,
        *,
        model_id: str,
        hf_repo: str,
        local_dir: str | None = None,
        device: str = "auto",
        dtype: str = "fp32",
        max_new_tokens: int = 512,
        **_: Any,
    ) -> None:
        _ensure_deps()
        if torch is None:
            raise RuntimeError(
                "torch is not installed; run `muse pull` or install "
                "`torch` into this venv"
            )
        if AutoModelForVision2Seq is None or AutoProcessor is None:
            raise RuntimeError(
                "transformers is not installed; run `muse pull` or "
                "install `transformers` into this venv"
            )
        self.model_id = model_id
        self._device = _select_device(device)
        self._dtype = _resolve_dtype(dtype)
        self._default_max_new_tokens = max_new_tokens
        src = local_dir or hf_repo
        with LoadTimer(logger, f"loading vision2seq from {src}"):
            self._processor = AutoProcessor.from_pretrained(src)
            self._model = AutoModelForVision2Seq.from_pretrained(
                src, torch_dtype=self._dtype,
            )
            self._model = self._model.to(self._device)
        set_inference_mode(self._model)

    def ocr(
        self,
        image: Any,
        *,
        prompt: str | None = None,
        max_new_tokens: int | None = None,
        num_beams: int = 1,
    ) -> OcrResult:
        """Extract text from one PIL.Image."""
        max_new = max_new_tokens or self._default_max_new_tokens
        inputs = self._processor(image, return_tensors="pt").to(self._device)
        gen_kwargs: dict[str, Any] = {
            "max_new_tokens": max_new,
            "num_beams": num_beams,
        }
        if prompt is not None:
            tok_ids = self._processor.tokenizer(
                prompt, return_tensors="pt",
            ).input_ids.to(self._device)
            gen_kwargs["decoder_input_ids"] = tok_ids
        outputs = self._model.generate(**inputs, **gen_kwargs)
        completion_tokens = int(outputs.shape[-1])
        # post_process_generation is a Nougat-specific helper that strips
        # control tokens and normalizes whitespace. When absent, we use
        # batch_decode directly.
        if hasattr(self._processor, "post_process_generation"):
            text = self._processor.post_process_generation(
                self._processor.batch_decode(outputs, skip_special_tokens=True)[0],
                fix_markdown=True,
            ) if "fix_markdown" in (
                self._processor.post_process_generation.__code__.co_varnames
                if hasattr(self._processor.post_process_generation, "__code__")
                else ()
            ) else self._processor.batch_decode(outputs, skip_special_tokens=True)[0]
        else:
            text = self._processor.batch_decode(outputs, skip_special_tokens=True)[0]
        return OcrResult(
            text=text, model_id=self.model_id,
            completion_tokens=completion_tokens,
        )


# Thin delegators preserved for test imports (matches sibling runtimes;
# the meta-test in tests/core/test_runtime_helpers_meta.py flags
# re-implementations).
def _select_device(device: str) -> str:
    return select_device(device, torch_module=torch)


def _resolve_dtype(dtype: str):
    return dtype_for_name(dtype, torch_module=torch)
```

- [ ] **B2: __init__.py**

```python
"""Generic runtimes for image/ocr."""
from muse.modalities.image_ocr.runtimes.hf_vision2seq import (
    HFVision2SeqRuntime,
)


__all__ = ["HFVision2SeqRuntime"]
```

- [ ] **B3: Tests**

Test sentinel-reset fixture, mocked AutoModelForVision2Seq + AutoProcessor; assert `ocr()` returns text, prompt forwarding builds decoder_input_ids, max_new_tokens override flows to generate kwargs, lazy-import discipline (raises without torch / transformers), local_dir preferred over hf_repo. Approximately 8 tests; mirror the v0.35.0 `test_hf_zero_shot.py` pattern.

- [ ] **B4: Run + commit**

```bash
pytest tests/modalities/image_ocr/runtimes/ tests/core/test_runtime_helpers_meta.py -q
git add src/muse/modalities/image_ocr/runtimes/ \
        tests/modalities/image_ocr/runtimes/
git commit -m "feat(image/ocr): HFVision2SeqRuntime

Generic OCR runtime over AutoModelForVision2Seq + AutoProcessor.
Works for TrOCR, Nougat, TexTeller, GOT-OCR. Optional prompt forward
(Nougat task tag), max_new_tokens override per call, beam search via
num_beams. Calls processor.post_process_generation when present
(Nougat LaTeX cleanup).

Closes #106 Task B."
```

## Task C: POST /v1/images/ocr route

**Files:**
- Create: `src/muse/modalities/image_ocr/routes.py`
- Test: `tests/modalities/image_ocr/test_routes.py`

**Steps:**

- [ ] **C1: Route**

`src/muse/modalities/image_ocr/routes.py`:

```python
"""FastAPI routes for /v1/images/ocr.

Multipart in (image file + optional model/prompt/max_new_tokens),
JSON out (id, model, text, usage). Mirrors /v1/audio/transcriptions.

Reuses decode_image_file from image_generation/image_input.py for
the multipart upload; the v0.34.0 MUSE_IMAGE_INPUT_MAX_BYTES env
cap applies. Per-backend _inference_lock from v0.34.0 serializes
calls into one model.
"""
from __future__ import annotations

import asyncio
import logging

from fastapi import APIRouter, File, Form, UploadFile
from fastapi.responses import JSONResponse

from muse.core.errors import ModelNotFoundError, error_response
from muse.core.registry import ModalityRegistry
from muse.modalities.image_generation.image_input import decode_image_file
from muse.modalities.image_ocr.codec import encode_ocr


MODALITY = "image/ocr"


logger = logging.getLogger(__name__)


_HARD_MAX_NEW_TOKENS = 4096


def build_router(registry: ModalityRegistry) -> APIRouter:
    router = APIRouter()

    @router.post("/v1/images/ocr")
    async def images_ocr(
        image: UploadFile = File(...),
        model: str | None = Form(None),
        prompt: str | None = Form(None),
        max_new_tokens: int | None = Form(None),
    ):
        # Cap validation: positive integer, bounded.
        if max_new_tokens is not None:
            if max_new_tokens < 1 or max_new_tokens > _HARD_MAX_NEW_TOKENS:
                return error_response(
                    400, "invalid_parameter",
                    f"max_new_tokens must be in [1, {_HARD_MAX_NEW_TOKENS}]; "
                    f"got {max_new_tokens}",
                )

        # Decode the multipart upload (size-capped via decode_image_file;
        # MUSE_IMAGE_INPUT_MAX_BYTES env cap applies).
        try:
            pil_image = await decode_image_file(image)
        except ValueError as e:
            return error_response(400, "invalid_parameter", str(e))

        try:
            backend = registry.get(MODALITY, model)
        except KeyError:
            raise ModelNotFoundError(
                model_id=model or "<default>", modality=MODALITY,
            )

        effective_id = backend.model_id

        def _call():
            with backend._inference_lock:
                kwargs = {}
                if prompt is not None:
                    kwargs["prompt"] = prompt
                if max_new_tokens is not None:
                    kwargs["max_new_tokens"] = max_new_tokens
                return backend.ocr(pil_image, **kwargs)

        try:
            result = await asyncio.to_thread(_call)
        except Exception as e:  # noqa: BLE001
            logger.exception("ocr call failed")
            return error_response(500, "internal_error", str(e))

        body = encode_ocr(result)
        # Override the result.model_id with the effective id in case the
        # runtime's model_id drifted from the registry key (paranoia).
        body["model"] = effective_id
        return JSONResponse(content=body)

    return router
```

- [ ] **C2: Route tests**

FakeModel + multipart upload tests: scalar success, missing model -> 404, missing image -> 422, oversized image (mock decode raise) -> 400, max_new_tokens validation, capability-flag passthrough doesn't gate (advisory only), prompt forward, runtime exception -> 500.

- [ ] **C3: Run + commit**

```bash
pytest tests/modalities/image_ocr/test_routes.py -q
git add src/muse/modalities/image_ocr/routes.py \
        tests/modalities/image_ocr/test_routes.py
git commit -m "feat(image/ocr): POST /v1/images/ocr route

Multipart in (image file + optional model/prompt/max_new_tokens),
JSON out (id, model, text, usage). Reuses decode_image_file from
image_generation/image_input.py (MUSE_IMAGE_INPUT_MAX_BYTES cap
applies). Per-backend _inference_lock serializes calls.

Closes #106 Task C."
```

## Task D: Bundled trocr-base-printed

**Files:**
- Create: `src/muse/models/trocr_base_printed.py`
- Test: `tests/models/test_trocr_base_printed.py`

**Steps:**

- [ ] **D1: Bundled script**

```python
"""Bundled muse model: trocr-base-printed.

English printed-text OCR (Microsoft, 334M, MIT). CPU-friendly default
for the image/ocr modality. Returns plain extracted text per image.

Wraps HFVision2SeqRuntime; the script declares the manifest and
joins the smoke-test matrix.
"""
from __future__ import annotations

from muse.modalities.image_ocr.runtimes import HFVision2SeqRuntime


MANIFEST = {
    "model_id": "trocr-base-printed",
    "modality": "image/ocr",
    "hf_repo": "microsoft/trocr-base-printed",
    "description": (
        "TrOCR base printed: 334M, English printed text, MIT. "
        "CPU-friendly default OCR for line-level extraction."
    ),
    "license": "MIT",
    "pip_extras": ["torch>=2.1.0", "transformers>=4.40.0", "Pillow"],
    "system_packages": [],
    "capabilities": {
        "device": "cpu",
        "memory_gb": 0.7,
        "max_new_tokens": 256,
        "supports_handwritten": False,
        "supports_math": False,
    },
}


class Model(HFVision2SeqRuntime):
    """All loading + inference logic in HFVision2SeqRuntime."""
    pass
```

- [ ] **D2: Test**

```python
from muse.models.trocr_base_printed import MANIFEST, Model


def test_manifest_required_keys():
    assert MANIFEST["model_id"] == "trocr-base-printed"
    assert MANIFEST["modality"] == "image/ocr"
    assert MANIFEST["hf_repo"] == "microsoft/trocr-base-printed"


def test_manifest_capabilities():
    caps = MANIFEST["capabilities"]
    assert caps["device"] == "cpu"
    assert caps["max_new_tokens"] == 256
    assert caps["supports_handwritten"] is False
    assert caps["supports_math"] is False


def test_model_inherits_runtime():
    from muse.modalities.image_ocr.runtimes import HFVision2SeqRuntime
    assert issubclass(Model, HFVision2SeqRuntime)


def test_pip_extras_includes_pillow():
    assert any("Pillow" in e for e in MANIFEST["pip_extras"])
```

- [ ] **D3: Run + commit**

```bash
pytest tests/models/test_trocr_base_printed.py -q
git add src/muse/models/trocr_base_printed.py \
        tests/models/test_trocr_base_printed.py
git commit -m "feat(models): bundled trocr-base-printed

Microsoft TrOCR base printed (334M, English, MIT). Reuses
HFVision2SeqRuntime; bundled to join the smoke matrix and serve as
the image/ocr modality default.

Closes #106 Task D."
```

## Task E: HF plugin

**Files:**
- Create: `src/muse/modalities/image_ocr/hf.py`
- Test: `tests/modalities/image_ocr/test_hf_plugin.py`

**Steps:**

- [ ] **E1: HF plugin**

```python
"""HF resolver plugin for image/ocr.

Sniffs `image-to-text` tag (the canonical HF tag for vision-encoder-
decoder models). Repo-name fallback for trocr / nougat / texteller
substrings catches checkpoints that ship without the canonical tag.

Does NOT claim `image-text-to-text` tag (those are VLMs, future
#97 image/description modality).

Priority 110: same slot as image_segmentation, audio_embedding.
Loaded via single-file import; no relative imports.
"""
from __future__ import annotations

from pathlib import Path
from typing import Iterable

from huggingface_hub import HfApi, snapshot_download

from muse.core.resolvers import ResolvedModel, SearchResult


_RUNTIME_PATH = (
    "muse.modalities.image_ocr.runtimes.hf_vision2seq:HFVision2SeqRuntime"
)
_PIP_EXTRAS = ("torch>=2.1.0", "transformers>=4.40.0", "Pillow")


def _model_id(repo_id: str) -> str:
    return repo_id.split("/", 1)[-1].lower()


def _repo_license(info) -> str | None:
    card = getattr(info, "card_data", None)
    if card is None:
        return None
    return getattr(card, "license", None)


def _sniff(info) -> bool:
    tags = getattr(info, "tags", None) or []
    if "image-to-text" in tags:
        # Exclude image-text-to-text VLMs (future #97 modality).
        if "image-text-to-text" in tags:
            return False
        return True
    repo_id = (getattr(info, "id", "") or "").lower()
    return any(s in repo_id for s in ("trocr", "nougat", "texteller"))


def _resolve(repo_id: str, variant: str | None, info) -> ResolvedModel:
    name = repo_id.lower()
    capabilities: dict = {"device": "auto"}
    if "handwritten" in name:
        capabilities["supports_handwritten"] = True
    if "nougat" in name or "texteller" in name or "math" in name:
        capabilities["supports_math"] = True

    manifest = {
        "model_id": _model_id(repo_id),
        "modality": "image/ocr",
        "hf_repo": repo_id,
        "description": f"OCR model: {repo_id}",
        "license": _repo_license(info),
        "pip_extras": list(_PIP_EXTRAS),
        "system_packages": [],
        "capabilities": capabilities,
    }

    def _download(cache_root: Path) -> Path:
        siblings = [s.rfilename for s in getattr(info, "siblings", [])]
        has_fp16 = any(".fp16." in f for f in siblings)
        allow_patterns = (
            ["*.fp16.safetensors", "*.json", "*.txt", "*.md"] if has_fp16
            else ["*.safetensors", "*.json", "*.txt", "*.md"]
        )
        # Older OCR repos still ship pytorch_model.bin (no safetensors).
        allow_patterns.append("pytorch_model.bin")
        # Tokenizer + processor files.
        allow_patterns.extend([
            "tokenizer*", "spiece.model", "preprocessor_config.json",
            "vocab.json", "merges.txt", "special_tokens_map.json",
        ])
        return Path(snapshot_download(
            repo_id=repo_id,
            allow_patterns=allow_patterns,
            cache_dir=str(cache_root) if cache_root else None,
        ))

    return ResolvedModel(
        manifest=manifest,
        backend_path=_RUNTIME_PATH,
        download=_download,
    )


def _search(api: HfApi, query: str, *, sort: str, limit: int) -> Iterable[SearchResult]:
    repos = api.list_models(
        search=query, filter="image-to-text",
        sort=sort, limit=limit,
    )
    for repo in repos:
        yield SearchResult(
            uri=f"hf://{repo.id}",
            model_id=_model_id(repo.id),
            modality="image/ocr",
            size_gb=None,
            downloads=getattr(repo, "downloads", None),
            license=None,
            description=repo.id,
        )


HF_PLUGIN = {
    "modality": "image/ocr",
    "runtime_path": _RUNTIME_PATH,
    "pip_extras": _PIP_EXTRAS,
    "system_packages": (),
    "priority": 110,
    "sniff": _sniff,
    "resolve": _resolve,
    "search": _search,
}
```

- [ ] **E2: Tests**

Cover: sniff true on `image-to-text`, sniff false on `image-text-to-text` (VLM exclusion), repo-name fallback, _resolve sets capabilities (handwritten name -> supports_handwritten=True; nougat name -> supports_math=True), download includes processor files, search yields results.

- [ ] **E3: Run + commit**

```bash
pytest tests/modalities/image_ocr/test_hf_plugin.py -q
git add src/muse/modalities/image_ocr/hf.py \
        tests/modalities/image_ocr/test_hf_plugin.py
git commit -m "feat(image/ocr): HF resolver plugin

Sniffs image-to-text tag (canonical HF tag for vision-encoder-
decoder OCR), with repo-name fallback for trocr/nougat/texteller
that some checkpoints lack the tag. Excludes image-text-to-text
(VLMs, future #97 modality).

Auto-detects supports_handwritten and supports_math from repo name.

Closes #106 Task E."
```

## Task F: Curated entries

**Files:**
- Modify: `src/muse/curated.yaml`

**Steps:**

- [ ] **F1: Add entries**

Append to `src/muse/curated.yaml`:

```yaml
# ---------- image/ocr ----------

- id: trocr-base-printed
  bundled: true

# Academic paper OCR including math and LaTeX. CC-BY-NC-4.0 (NON-COMMERCIAL):
# fine for personal research use; not suitable for distribution in commercial
# products. Pull explicitly via `muse pull nougat-base`.
- id: nougat-base
  uri: hf://facebook/nougat-base
  modality: image/ocr
  size_gb: 0.5
  description: "Nougat base: 250M, academic papers (math/LaTeX), CC-BY-NC-4.0 (non-commercial)"
  capabilities:
    memory_gb: 1.0
    max_new_tokens: 4096
    supports_math: true

# Math-formula-only LaTeX OCR. MIT.
- id: texteller
  uri: hf://OleehyO/TexTeller
  modality: image/ocr
  size_gb: 0.8
  description: "TexTeller: 400M, math-formula -> LaTeX, MIT"
  capabilities:
    memory_gb: 1.4
    max_new_tokens: 1024
    supports_math: true

# Handwriting recognition. ~558M; bigger than the bundled printed model.
- id: trocr-large-handwritten
  uri: hf://microsoft/trocr-large-handwritten
  modality: image/ocr
  size_gb: 1.1
  description: "TrOCR large handwritten: 558M, handwritten English text, MIT"
  capabilities:
    memory_gb: 1.5
    max_new_tokens: 256
    supports_handwritten: true
```

- [ ] **F2: Verify load + commit**

```bash
python -c "
from muse.core.curated import find_curated
for cid in ['trocr-base-printed', 'nougat-base', 'texteller', 'trocr-large-handwritten']:
    e = find_curated(cid)
    print(f'{cid}: {\"FOUND\" if e else \"NOT FOUND\"}')
"
pytest tests/core/test_curated.py -q
git add src/muse/curated.yaml
git commit -m "feat(curated): 4 image/ocr entries for v0.36.0

One bundled alias (trocr-base-printed) plus three URI-resolved
alternatives covering academic OCR (nougat-base), math-only
(texteller), and handwriting (trocr-large-handwritten). nougat-base
license is CC-BY-NC-4.0; flagged in description.

Closes #106 Task F."
```

## Task G: Smoke matrix + docs

**Files:**
- Modify: `.github/workflows/fresh-venv-smoke.yml`
- Modify: `CLAUDE.md`

**Steps:**

- [ ] **G1: Smoke matrix**

Add `trocr-base-printed` to the matrix list.

- [ ] **G2: CLAUDE.md**

In the Project overview, add a bullet:

```markdown
- **image/ocr**: image-to-text via `/v1/images/ocr` (trocr-base-printed bundled; Nougat / TexTeller / handwritten-TrOCR family via the resolver; multipart upload + OpenAI-shape envelope mirroring `/v1/audio/transcriptions`)
```

In the modality conventions section, add a paragraph noting that `image/ocr` excludes `image-text-to-text` tags (those are VLMs reserved for the future #97 image/description modality).

- [ ] **G3: Commit**

```bash
git add .github/workflows/fresh-venv-smoke.yml CLAUDE.md
git commit -m "docs(image/ocr): v0.36.0 docs + smoke matrix

CLAUDE.md adds the new modality bullet and notes the explicit
non-claim of image-text-to-text VLMs (reserved for future #97).
The smoke matrix gains trocr-base-printed.

Closes #106 Task G."
```

## Final: release v0.36.0

- [ ] Bump version in `pyproject.toml`: `0.35.0` -> `0.36.0`.
- [ ] Run full fast-lane suite: `pytest tests/ -q -m "not slow"`. Expect ~+30-40 new tests.
- [ ] Commit + tag + push:

```bash
git add pyproject.toml
git commit -m "chore(release): v0.36.0

New image/ocr modality: POST /v1/images/ocr (multipart in, JSON out).
One bundled (trocr-base-printed); three curated (nougat-base,
texteller, trocr-large-handwritten). HFVision2SeqRuntime over
AutoModelForVision2Seq + AutoProcessor.

Closes #106 (image/ocr)."
git tag v0.36.0
git push origin main && git push origin v0.36.0
```

- [ ] `gh release create v0.36.0` with notes.

---

## Self-review checklist

- **Spec coverage:** Tasks A-G map onto the spec sections (wire shape: A+C; runtime: B; bundled: D; HF plugin: E; curated: F; smoke+docs: G). Release: separate commit.
- **Placeholder scan:** Task B has the `post_process_generation` introspection logic that's a bit clever; the implementer should write it carefully and add a test. Task C's route is full. Task E's HF plugin is full.
- **Type consistency:** `OcrResult` shape (text, model_id, completion_tokens, metadata) matches across protocol, codec, runtime, route, client.
- **Em-dashes:** check before commit (hook blocks).
